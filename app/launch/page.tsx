import LaunchExhibit from "@/components/exhibit/LaunchExhibit";

export const metadata = { title: "The Launch — CUDA Visual Encyclopedia" };

export default function LaunchPage() {
  return (
    <main>
      <LaunchExhibit />
    </main>
  );
}
